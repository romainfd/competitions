all: *.py *.txt
	python pb$(PROG).py < pb$(PROG).txt

create:
	touch pb$(PROG).py
	echo "# Code created by Romain Fouilland for problem:\n# \n" > pb$(PROG).py
	echo "t = int(input())\n\nfor _ in range(t):\n\t" >> pb$(PROG).py
	touch pb$(PROG).txt
